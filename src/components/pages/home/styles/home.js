import styled from "styled-components";

const HomeSocialMedia = styled.div`
  display: flex;
  padding: 1rem;
`;

const HomeBoxIcon = styled.a`
  padding: 1rem;
`;

export { HomeSocialMedia, HomeBoxIcon };
